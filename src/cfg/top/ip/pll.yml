
import : clk settings

type  : clk_wiz

config:
    PRIMITIVE                  : PLL
    PRIM_IN_FREQ               : = clk.REF_CLK_FREQ
    CLKOUT1_REQUESTED_OUT_FREQ : = clk.MAIN_CLK_FREQ
    USE_LOCKED                 : true
    USE_RESET                  : false
    USE_SAFE_CLOCK_STARTUP     : true
 
 

